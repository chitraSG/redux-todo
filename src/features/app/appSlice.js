import { createSlice } from "@reduxjs/toolkit";

/** Replace with your own slices (recipes, filters, etc.) */
const appSlice = createSlice({
  name: "app",
  initialState: {},
  reducers: {},
});

export default appSlice.reducer;
